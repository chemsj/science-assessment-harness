# 수행기록/ — 실시 단계 (평가 프로토콜 단계 2)

- 여기에 `관찰기록-<반>.csv`를 둔다 (예: `관찰기록-1반.csv`).
- 형식(4열 고정): `번호,날짜,관찰한 사실,관련 역량` — 규격은 `references/관찰기록-양식.md`.
- **비식별**: 번호만. 실명·학번 금지 (lint가 검사).
- 즉석 메모를 표로 바꿀 때는 `관찰기록` 스킬 사용.
