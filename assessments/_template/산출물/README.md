# 산출물/ — 결과 정리 단계 (평가 프로토콜 단계 3)

- `채점표-<반>.csv` — `채점표` 스킬이 생성. 근거는 `수행기록/관찰기록-<반>.csv` + `루브릭.json` **뿐**.
- `과세특-초안-<반>.md` — `과세특연계` 스킬이 생성. '사실 → 역량' 변환, 번호별 `##` 섹션.
- 모든 파일은 **초안** — 점수·문장의 확정과 `[확인 필요]` 해소는 교사가 한다.
